#include <stdio.h>\n#include <stdlib.h>\n\nvoid appendToFile(const char *filename, const char *data) {\n    FILE *file = fopen(filename, "a"); // Open file in append mode\n    if (file == NULL) {\n        fprintf(stderr, "Error opening file!\n");\n        return;\n    }\n    fprintf(file, "%s\n", data); // Append data\n    fclose(file); // Close the file\n}\n\nint main() {\n    const char *filename = "example.txt"; // Specify the file to append to\n    const char *data = "This is appended data."; // Data to append\n    appendToFile(filename, data);\n    return 0;\n}